from setuptools import setup, find_packages

setup(
    name="shared_utils",
    version="1.0.0",
    packages=find_packages(),
    description="Reusable Spark transformations and configuration drivers for Data Platform Engineering",
    author="Data Architecture Team",
    python_requires=">=3.9",
    install_requires=[],
)